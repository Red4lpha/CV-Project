import React, { Fragment, useState } from 'react';
import ExpViewer from "./ExpViewer";

    const Experience = () => {
        return (
            <ExpViewer />
        );
    };

export default Experience;